package main

import "fmt"

func main() {
	fmt.Println("unimplemented")
	fmt.Println("make a shell that uses the embedded ruby binary to create an irb-like experience")
	fmt.Println("its not an issue that we could just embed irb, but embedding ruby allows us to ")
	fmt.Println("develop as if we had the rubyvm complete, working on other components while the ")
	fmt.Println("GoRuby VM is being completed")
}
